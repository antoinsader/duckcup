[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "auth"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["core", "requests", "telethon"]

[tool.hatch.build.targets.wheel]
packages = ["auth"]
